require 'spec_helper'

describe EventosController do

  #Delete this example and add some real ones
  it "deveria gerar um arquivo icalendar para os eventos que estão por vir" do
      fixtures :qcon, :ruby_conf
      p "#{qcon.nome}---------"
      eventos = []
#      evento << 
  end

end
